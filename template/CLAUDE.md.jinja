# {{ project_name }} — Claude Code extras

<!-- forge:start — DO NOT EDIT between forge markers (updated by copier update) -->

@AGENTS.md
<!-- jv-entry-contract-expected: 2 -->

> `AGENTS.md` (imported above) is the canonical entry contract for every runtime. This file adds only what is
> specific to Claude Code and never restates the contract.

## Slash commands

| Contract step | Claude Code command |
|-|-|
| Design the work | `/scope` |
| Verify the work / loop stage 5 (review gate) | `/check`, `/verify:complete` |
| Session transitions (`work` skill) | `/work:handoff`, `/work:pause`, `/work:continue` |

## Hooks

The stop pipeline (`.claude/hooks/stop/`) enforces the TDD gate and reads the `[PHASE_COMPLETE]` signal;
guards run on write and migration tool calls. Skills are read from `.claude/skills/` (the generated projection).

<!-- forge:end -->
