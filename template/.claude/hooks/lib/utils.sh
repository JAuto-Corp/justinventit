#!/bin/bash
# Claude Code compatibility adapter for provider-neutral hook utilities.

JUSTINVENTIT_CLAUDE_UTILS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$JUSTINVENTIT_CLAUDE_UTILS_DIR/../../../.agents/hooks/lib/utils.sh"
