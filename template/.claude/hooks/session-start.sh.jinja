#!/bin/bash
# Claude Code adapter for the provider-neutral justinventit session-start hook.

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/../../.agents/hooks/session-start.sh"
